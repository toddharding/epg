defmodule Epg.Test.PrintTable do
  use ExUnit.Case

  test "console output is correct when input list is symmetrical i.e. N x N" do
    assert false
  end

  test "no output if empty list is given" do
    assert false
  end

  test "input must be a list" do
    assert false
  end
end